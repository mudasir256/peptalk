import { StyleSheet, Text, View } from "react-native";
import React from "react";

const useMediaUpload = () => {
  return (
    <View>
      <Text>useMediaUpload</Text>
    </View>
  );
};

export default useMediaUpload;

const styles = StyleSheet.create({});
