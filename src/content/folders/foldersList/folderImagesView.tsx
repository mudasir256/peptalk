import React from "react"
import { View } from "react-native"

type Props = {
  images: string[]
}

export const FolderImagesView = () => {
  return <View style={{ backgroundColor: "blue" }}></View>
}